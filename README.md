# http-server-from-scratch
Учебный HTTP/1.1 сервер на raw TCP (Python 3.12+): incremental парсер, serializer, routing {param}, keep-alive, timeouts, selectors loop и TLS через ssl.SSLContext — без Flask/FastAPI/asyncio. 171 тест, mypy --strict.
